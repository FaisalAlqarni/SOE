---
name: invalid-skill
description:
---

# Invalid Skill

This fixture has a `name` but an EMPTY `description`, so the checker must
FAIL on it. It proves the test catches frontmatter violations (RED state).
