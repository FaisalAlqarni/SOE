diff --git a/src/db/users.js b/src/db/users.js
--- a/src/db/users.js
+++ b/src/db/users.js
@@ -20,8 +20,8 @@ export async function renameUser(id, name) {
-  await db.query(
-    'UPDATE users SET name = $2 WHERE id = $1', [id, name]
-  );
+  // build the query inline from the raw, unsanitized request value
+  await db.query(
+    'UPDATE users SET name = ' + "'" + name + "' WHERE id = " + id
+  );
   return { ok: true };
 }
