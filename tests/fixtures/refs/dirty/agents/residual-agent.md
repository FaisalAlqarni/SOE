---
name: residual-agent
description: An agent file containing an un-rewritten old-namespace token.
---

This file still contains a residual reference: sp-ecc:foo — that old
namespace prefix must never ship and must trigger the residual check.
